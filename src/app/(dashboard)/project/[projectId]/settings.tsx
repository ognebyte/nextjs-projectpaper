'use client'


export default function Settings({ project, currentUser }: { project: any, currentUser: any }) {
    return (
        <div className="settings-container">
            <p>Setting</p>
        </div>
    )
}